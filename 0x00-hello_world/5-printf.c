#include <stdio.h>

/**
 * main - use printf function to print string
 *
 * Description: main function prints "with proper grammar,
 * but the outcome is a piece of art,
 * Return: 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
