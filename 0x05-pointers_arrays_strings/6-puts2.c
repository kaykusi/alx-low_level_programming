#include "holberton.h"

/**
 * puts2 - prints each character
 * @str: char pointer
 * Return: void
 */

void puts2(char *str)
{
	int count, scan = 0;

	while (str[count] != '\0')
	{
		count++;
	}

	for (b = 0; b < a; b++)
	{
		if (b % 2 == 0)
		{
			_putchar(str[b]);
		}
	}
	_putchar('\n');
}
