#include "main.h"

/**
 * _abs - prints absolute value of a number
 *
 * @n: integer to be computed
 * Return: integer value
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
